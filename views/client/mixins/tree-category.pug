mixin tree-category(categorys)
    if categorys.length > 0
        ul
            each category in categorys
                li
                    a.nav-link(href=`products/${category.slug}`) #{category.title}
                    if category.childen && category.childen.length > 0
                        +tree-category(category.childen)
