extends ../../layouts/defaut.pug
include ../../mixins/search-and-apply.pug
include ../../mixins/product-dustbin.pug
include ../../mixins/pagination.pug
block main
    h1 Thùng rác tài khoản
    if roleLogin.permission.includes("account_view-dustbin")
        .container.mt-4
            .row.justify-content-center
                .col-md-8
                    // Thanh tìm kiếm
                    form(id="form-search")
                        .input-group.mb-4
                            input.form-control(
                                type="text" 
                                placeholder="Tìm kiếm tài khoản..." 
                                name="keyword"  
                                value=keyword 
                                )
                            button.btn.btn-outline-secondary(type="button" id="button-clear")
                                i.fa.fa-times 
                            .input-group-append
                                button.btn.btn-primary(type="submit")
                                    i.bi.bi-search
                                    | Tìm kiếm
                    // Chọn và nút áp dụng
                    if roleLogin.permission.includes("account_edit-dustbin")   
                        form(action=`${frefixAdmin}/dustbinAccount/change-multi?_method=PATCH` method="post" form-option)
                            .input-group
                                select.form-control(name="type" select-option)
                                    option(disabled selected) --Chọn hành động--
                                    option(value="restore") Khôi phục
                                    option(value="delete") Xóa vĩnh viễn
                                .input-group-append
                                    button.btn.btn-success(type="submit") Áp dụng
                                    .form-group.mr-2
                            input.form-control.d-none(
                                type="text"
                                name="ids"
                                value=""
                                placeholder="San pham ap dung"
                                input-submit
                            )
            if accounts.length > 0 
                .row.mt-4
                    .col-md-12
                        table.table.table-striped.table-bordered
                            thead.thead-dark
                                tr
                                    th
                                        input(type="checkbox" name="checkall")
                                    th.text-center(w-10) STT
                                    th.text-center(w-20) Họ và tên
                                    th.text-center(w-20) Hình ảnh
                                    th.text-center(w-20) Ngày xóa
                                    th.text-center(w-20) Người xóa
                                    th.text-center(w-20) Hành động
                            tbody
                                each item , index in accounts
                                    tr
                                        td  
                                            input(type="checkbox" name="id" id=item.id)
                                        td.text-center #{index + 1}
                                        td.text-center #{item.fullName}
                                        td.text-center
                                            img(src=item.avartar, alt=item.title, width='50')
                                        td.text-center 
                                            if item.deletedBy.dateDeleted
                                                - const date = moment(item.deletedBy.dateDeleted).format("DD/MM/YYYY HH:mm:ss")
                                                p #{date}
                                        td.text-center 
                                            if item.deletedBy.nameAccountDeleted
                                                p #{item.deletedBy.nameAccountDeleted}
                                            else 
                                                p Không có
                                        td.text-center
                                            if roleLogin.permission.includes("account_edit-dustbin")
                                                button(
                                                    class="btn btn-primary mr-2"
                                                    id-account = item.id
                                                    restore-account
                                                ) Khôi phục
                                                button(
                                                    class="btn btn-danger"
                                                    id-account = item.id 
                                                    delete-account
                                                ) Xóa vĩnh viễn 
                                            else 
                                                h6 Bạn không được thực hiện hành động nào
            else
                .container.mt-5
                    .row.justify-content-center
                        .col-md-6
                            .alert.alert-warning.text-center
                                i.bi.bi-exclamation-triangle-fill 
                                | Thùng rác rỗng

            form(action=""  method="POST" id="form-restore" path=`${frefixAdmin}/dustbinAccount/restore`)
            form(action=""  method="POST" id="form-delete" path=`${frefixAdmin}/dustbinAccount/delete`)
        +pagination(pagination)
        script(src="/admin/js/dustbinAccount.js") 
    else 
        h2 Bạn không có quyền truy cập vào trang này
